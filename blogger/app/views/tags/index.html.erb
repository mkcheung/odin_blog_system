<h1>All Tags</h1>
<ul>
	<% @tags.each do |tag| %>
		<li>
			<%=link_to tag.name,tag_path(tag)%> 
			<%= link_to "Delete", tag_path(tag),data:{delete:"Confirm tag delete?"}, method: :delete%>
		</li>
	<%end%>
</ul>